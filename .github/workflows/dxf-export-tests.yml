name: DXF export regression
on:
  push:
    paths:
      - 'tools/export_dxf.py'
      - 'tools/DXF_INPUT_SCHEMA.md'
      - 'tests/**'
      - 'requirements.txt'
      - '.github/workflows/dxf-export-tests.yml'
  pull_request:
    paths:
      - 'tools/**'
      - 'tests/**'
      - 'requirements.txt'
jobs:
  serializer:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install -r requirements.txt
      - run: python -m pytest tests/ -q
