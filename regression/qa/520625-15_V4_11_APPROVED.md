# V4.11 approved QA / DRAW → FLAG → CONFIRM — 520625-15

**Direct user approval:** 2026-09-26. The user reports QA PASS on the entire previously delivered seven-code job `520625-15`. This documents user QA of **that delivery**, not new independent validation of any subsequently modified/exported DXF.

## Drawing and code order

The PDF has a page-1 job list and seven corresponding drawing sheets. The approved order in the one top-to-bottom composite is:

1. `036583` — PDF page 2
2. `036571` — PDF page 3
3. `036572` — PDF page 4
4. `036573` — PDF page 5
5. `036574` — PDF page 6
6. `036575` — PDF page 7
7. `036576` — PDF page 8

## Confirmed `036575` feature relationship

The disk has `Ø300` outer diameter. The user confirmed **two Ø5 holes are diametrically opposed on one diameter, with 180 mm between their centers**, and **one Ø9 hole is at the disk center**. In local disk-centered coordinates the Ø5 pair is at signed radius `+90` and `−90 mm` along the *drawing-shown* diameter; the Ø9 center is `(0,0)`. Do not invent a global angular orientation or transfer these part-specific dimensions to another code. The question whether these three holes have this relationship is **resolved** for `036575`.

## Universal approved workflow

For ANY future sheet with uncertainty, do not silently omit a recognizable part. Interpret the PDF first and keep proven geometry exact. When the drawing supports a plausible provisional depiction, **DRAW FIRST**: render the uncertain element in Magenta (Layer 0, color 6), mark the precise assumed dimension/type/topology with an adjacent Magenta warning, and add a code-specific FLAG and targeted question. Truly estimated numeric values are integer-rounded; proven numbers preserve full precision.

When a defensible flat cannot be established, present independently proved orthographic/source views as separate, explicitly labeled groups under the correct part code (`VIEWS_ONLY`) and FLAG the required missing relationship. Do not invent a flat merely to eliminate a question. Only when no recognizable view or code mapping exists should drawing stop pending clarification.

The default deliverable remains **ONE** top-to-bottom, code-labeled composite. Any unresolved estimate/source-view-only part makes the **entire combined file** `*_ALL_REVIEW_ONLY.dxf` with a global **NO CUT** annotation. An answer confirming a FLAG closes that FLAG only; re-run all actual Section 5 manufacturing checks and saved-DXF read-back before any updated production PASS.

## Mandatory regression assertions

- `036575` has exactly two Ø5 holes separated by 180 mm on a diameter and exactly one center Ø9; this *already confirmed* relationship itself does not remain flagged.
- Proven dimensions/features elsewhere are unchanged by a new uncertainty or preview.
- Any uncertain but recognizable source-supported shape yields an explicitly approximate Magenta preview or separated labeled source views, rather than omission.
- Uncertain shape, hole identity, datum, bend order, or numeric estimate **cannot** be marked production PASS without resolution and verified checks.
- User QA of the previous seven-code delivery never certifies a later geometry change without re-validation.
